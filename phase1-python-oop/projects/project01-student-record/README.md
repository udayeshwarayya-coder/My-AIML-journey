# Project 01 — Student Report Tracker
**Phase 1 | Days 1-4 | Python OOP Basics**

> ??? *learning with ai #1*

## ?? What It Does
A full-stack web app to manage student records — add students, view grades, search, sort, and generate report cards.

## ?? Concepts Used
- Classes & Objects (`__init__`, `self`)
- Instance variables & Class variables
- Multiple objects & Object interaction
- Flask REST API (GET/POST/DELETE)

## ?? Structure
```
project01-student-record/
+-- backend/
¦   +-- app.py          ? Flask API with all student logic
+-- frontend/
    +-- index.html      ? Full-stack UI (AI-generated)
```

## ?? How to Run
```bash
cd backend
pip install flask flask-cors
python app.py
# Open frontend/index.html in browser
```
